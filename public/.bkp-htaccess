<IfModule mod_rewrite.c>
    RewriteEngine On

    # REQUIRED for cPanel + PHP-FPM
    SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
